# Backend used by the Vite development proxy.
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Local frontend-only login. Development mode only; any email/password becomes an admin.
VITE_ENABLE_ADMIN_MOCK=false
